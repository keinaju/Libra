extends layout

block content

  h1 #{title}: #{book_copy._id}

  p Do you really want to delete this copy?

  form(method='POST')
    div.form-group
      input#bookinstanceid.form-control(type='hidden', name='bookinstanceid', value=book_copy._id )

    button.btn.btn-primary(type='submit') Delete
